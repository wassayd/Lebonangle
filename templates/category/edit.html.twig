{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <h1 class="jumbotron">Editer la categorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success my-2"><i class="fa fa-edit"></i></button>
    {{ form_end(form) }}

    {{ include('category/_delete_form.html.twig') }}

    <a href="{{ path('category_index') }}" class="btn btn-primary mt-3"><i class="fa fa-arrow-circle-left"></i> Retour</a>
{% endblock %}
